#!/usr/bin/python3
def print_last_digit(number):
    if number <0:
        last-digit=number %-(10)
        print(-(last-digit), end=")
        else:
        last-digit = number % 10
        print(last-digit, end=")
        return abs(last_digit)
